name: Build Ossis Remote Control

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: ossis
    secrets: inherit
